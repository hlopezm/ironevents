<%= simple_form_for @event do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :start_at, as: :datepicker %>
  <%= f.input :end_at, as: :datepicker %>
  <%= f.button :submit %>
<% end %>
